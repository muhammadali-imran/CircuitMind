# CircuitMind — Production Dependencies
# Install with: pip install -r requirements.txt

python-dotenv>=1.0.0
redis>=5.0.0
pydantic-settings>=2.0.0

# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
python-multipart>=0.0.9

# Rate limiting
slowapi>=0.1.9

# LLM — direct Groq SDK, still used inside generate/llm_generate.py and
# hint/llm_hint.py until those specifically are migrated to ChatGroq
groq>=0.4.0

# LangChain — the gateway agent (agent/)
# Pinned to the 1.x line: agent/executor.py uses langchain.agents.create_agent,
# which only exists from LangChain 1.0 onward (the old create_tool_calling_agent
# / AgentExecutor API was removed, not just deprecated).
langchain>=1.0.0
langchain-core>=1.0.0
langchain-groq>=0.2.0
langgraph>=0.2.0

# Circuit export
schemdraw>=0.19

# Web UI
streamlit>=1.35.0
requests>=2.32.0
