# CircuitMind — Production Dependencies
# Install with: pip install -r requirements.txt

python-dotenv>=1.0.0
redis>=5.0.0
pydantic-settings>=2.0.0

# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
python-multipart>=0.0.9

# Rate limiting
slowapi>=0.1.9

# LLM — direct Groq SDK, still used inside generate/llm_generate.py and
# hint/llm_hint.py until those specifically are migrated to ChatGroq
groq>=0.4.0

# LangChain — the gateway agent (agent/)
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.0
langchain-community>=0.2.0

# Circuit export
schemdraw>=0.19

# Web UI
streamlit>=1.35.0
requests>=2.32.0
